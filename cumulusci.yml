minimum_cumulusci_version: '3.93.0'
project:
    name: Q365-Implementation
    package:
        name: Q365-Implementation
        api_version: '62.0'
    git:
        default_branch: 'main'
    source_format: sfdx

    dependencies:
        # Q!365
        - version_id: '04t69000001WiiuAAC'

tasks:
    run_tests:
        options:
            required_org_code_coverage_percent: 75
